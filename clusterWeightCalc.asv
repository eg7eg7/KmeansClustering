function [Clusters, cluster_changes] = clusterWeightCalc(INDX,DataMATRIX)

%   Clusters is weight matrix - n rows (dimension) and K columns (number of clusters) 
%   cluster_changes is the percentage of change the weights 
%
%
%